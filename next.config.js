// next.config.js
module.exports = {
    reactStrictMode: true,  // Ensures React strict mode is enabled
    exportTrailingSlash: true,  // Optional: Ensures trailing slashes on URLs
    images: {
      unoptimized: true,  // Allow image optimization to work with static exports
    },
  }
  