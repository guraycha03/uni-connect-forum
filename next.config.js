// next.config.js


// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // Optional: Only if you're not using optimized images
  },
};

module.exports = nextConfig;

